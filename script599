local Players = game:GetService("Players")
local StarterPack = game:GetService("StarterPack")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- متغیر تاخیر
local giveToolDelay = 0

-- تنظیم زمان رسپاون به 0
Players.RespawnTime = 0 

-- ساخت GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ClassicAdminPanel"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
-- ارتفاع فریم رو برای جا شدن اسلایدر بیشتر کردیم (از 140 به 200)
MainFrame.Size = UDim2.new(0, 220, 0, 200)
MainFrame.Position = UDim2.new(0.5, -110, 0.5, -70)
MainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
MainFrame.BorderSizePixel = 2
MainFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.Active = true
MainFrame.Draggable = true 
MainFrame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Title.BorderSizePixel = 1
Title.BorderColor3 = Color3.fromRGB(0, 0, 0)
Title.Text = "C00LDUPER"
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 18
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Parent = MainFrame

local function createButton(name, text, yPos)
	local btn = Instance.new("TextButton")
	btn.Name = name
	btn.Size = UDim2.new(0.85, 0, 0, 35)
	btn.Position = UDim2.new(0.075, 0, 0, yPos)
	btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
	btn.BorderSizePixel = 1
	btn.BorderColor3 = Color3.fromRGB(0, 0, 0)
	btn.Text = text
	btn.Font = Enum.Font.SourceSansBold
	btn.TextSize = 16
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.AutoButtonColor = true
	btn.Parent = MainFrame
	return btn
end

local CheckToolsBtn = createButton("CheckToolsBtn", "Check Tools", 40)
local DupeToolsBtn = createButton("DupeToolsBtn", "Dupe Tools", 85)

-- ================== ساخت بخش اسلایدر ==================
local DelayLabel = Instance.new("TextLabel")
DelayLabel.Size = UDim2.new(0.85, 0, 0, 20)
DelayLabel.Position = UDim2.new(0.075, 0, 0, 135)
DelayLabel.BackgroundTransparency = 1
DelayLabel.Text = "Give Tool Delay: 0s"
DelayLabel.Font = Enum.Font.SourceSansBold
DelayLabel.TextSize = 15
DelayLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
DelayLabel.Parent = MainFrame

local SliderBG = Instance.new("TextButton")
SliderBG.Size = UDim2.new(0.85, 0, 0, 10)
SliderBG.Position = UDim2.new(0.075, 0, 0, 165)
SliderBG.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
SliderBG.BorderSizePixel = 1
SliderBG.BorderColor3 = Color3.fromRGB(0, 0, 0)
SliderBG.Text = ""
SliderBG.AutoButtonColor = false
SliderBG.Parent = MainFrame

local SliderKnob = Instance.new("Frame")
SliderKnob.Size = UDim2.new(0, 12, 0, 20)
SliderKnob.AnchorPoint = Vector2.new(0.5, 0.5)
SliderKnob.Position = UDim2.new(0, 0, 0.5, 0)
SliderKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
SliderKnob.BorderSizePixel = 1
SliderKnob.BorderColor3 = Color3.fromRGB(0, 0, 0)
SliderKnob.Parent = SliderBG

-- سیستم کشیدن اسلایدر
local isDragging = false

local function updateSlider(input)
	local relativeX = math.clamp((input.Position.X - SliderBG.AbsolutePosition.X) / SliderBG.AbsoluteSize.X, 0, 1)
	SliderKnob.Position = UDim2.new(relativeX, 0, 0.5, 0)
	
	-- محاسبه مقدار تاخیر بین 0 تا 5 (با یک رقم اعشار)
	giveToolDelay = math.floor((relativeX * 5) * 10) / 10
	DelayLabel.Text = "Give Tool Delay: " .. tostring(giveToolDelay) .. "s"
end

SliderBG.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		isDragging = true
		updateSlider(input)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		isDragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		updateSlider(input)
	end
end)
-- =======================================================

local function notify(title, text)
	game.StarterGui:SetCore("SendNotification", {
		Title = title;
		Text = text;
		Duration = 4;
	})
end

CheckToolsBtn.MouseButton1Click:Connect(function()
	local toolsToCheck = {}
	local backpack = LocalPlayer:FindFirstChild("Backpack")
	if backpack then
		for _, item in pairs(backpack:GetChildren()) do
			if item:IsA("Tool") then table.insert(toolsToCheck, item) end
		end
	end
	if LocalPlayer.Character then
		for _, item in pairs(LocalPlayer.Character:GetChildren()) do
			if item:IsA("Tool") then table.insert(toolsToCheck, item) end
		end
	end
	
	if #toolsToCheck == 0 then
		notify("Check", "هیچ ابزاری پیدا نشد!")
		return
	end
	
	for _, tool in ipairs(toolsToCheck) do
		if not tool.CanBeDropped then
			notify("❌ " .. tool.Name, "صد در صد قابل کلون شدن نیستند")
		elseif tool.CanBeDropped and not StarterPack:FindFirstChild(tool.Name) then
			notify("⚠️ " .. tool.Name, "به احتمال 40 درصد کلون میشود")
		else
			notify("✅ " .. tool.Name, "ابزار عادی است و در StarterPack وجود دارد")
		end
	end
end)

-- عملکرد Dupe Tools
local grabtoolsFunc = nil
local isDupeOnCooldown = false

DupeToolsBtn.MouseButton1Click:Connect(function()
	if isDupeOnCooldown then return end
	
	local char = LocalPlayer.Character
	if not char then return end
	
	local hrp = char:FindFirstChild("HumanoidRootPart")
	local humanoid = char:FindFirstChild("Humanoid")
	if not hrp or not humanoid then return end
	
	isDupeOnCooldown = true
	DupeToolsBtn.Text = "Please Wait..."
	DupeToolsBtn.BackgroundColor3 = Color3.fromRGB(120, 50, 50)
	
	local savedCFrame = hrp.CFrame
	local cam = workspace.CurrentCamera
	
	local camDummy = Instance.new("Part")
	camDummy.Transparency = 1
	camDummy.Anchored = true
	camDummy.CanCollide = false
	camDummy.CFrame = hrp.CFrame
	camDummy.Parent = workspace
	cam.CameraSubject = camDummy
	
	local backpack = LocalPlayer:FindFirstChild("Backpack")
	if backpack then
		for _, v in pairs(backpack:GetChildren()) do
			if v:IsA("Tool") then v.Parent = char end
		end
	end
	
	local skyPlatform = Instance.new("Part")
	skyPlatform.Anchored = true
	skyPlatform.Size = Vector3.new(100, 5, 100)
	skyPlatform.CFrame = savedCFrame + Vector3.new(0, 14995, 0)
	skyPlatform.Transparency = 1
	skyPlatform.Parent = workspace
	
	char:PivotTo(savedCFrame + Vector3.new(0, 15000, 0))
	task.wait(0.3) 
	
	for _, v in pairs(char:GetChildren()) do
		if v:IsA("Tool") then v.Parent = workspace end
	end
	
	skyPlatform:Destroy()
	humanoid.Health = 0
	
	local newChar = LocalPlayer.CharacterAdded:Wait()
	local newHrp = newChar:WaitForChild("HumanoidRootPart", 3)
	local newHumanoid = newChar:WaitForChild("Humanoid", 3)
	
	cam.CameraSubject = newHumanoid
	camDummy:Destroy()
	
	if newHrp then
		local endTime = tick() + 0.3
		local connection
		connection = RunService.RenderStepped:Connect(function()
			if tick() < endTime and newHrp.Parent then
				newHrp.CFrame = savedCFrame
			else
				if connection then connection:Disconnect() end
			end
		end)
	end
	
	if newHumanoid then
		-- شروع پروسه گرفتن ابزارها به صورت موازی (Spawn)
		task.spawn(function()
			-- اعمال تاخیر درگ شده از اسلایدر
			if giveToolDelay > 0 then
				task.wait(giveToolDelay)
			end
			
			-- پیدا کردن لوکیشن دقیق کاراکتر شما در حال حاضر (اگه تو زمان تاخیر راه رفته باشی)
			local currentCFrame = newHrp and newHrp.CFrame or savedCFrame
			
			for _, child in ipairs(workspace:GetChildren()) do
				if child:IsA("Tool") and child:FindFirstChild("Handle") then
					child.Handle.CFrame = currentCFrame 
					newHumanoid:EquipTool(child)
				end
			end

			if grabtoolsFunc then grabtoolsFunc:Disconnect() end

			grabtoolsFunc = workspace.ChildAdded:Connect(function(child)
				if newChar and child:IsA("Tool") and child:FindFirstChild("Handle") then
					-- هر ابزار جدید هم دقیقاً میاد به دست شما در حال حرکت
					child.Handle.CFrame = newHrp and newHrp.CFrame or savedCFrame
					newHumanoid:EquipTool(child)
				end
			end)
			
			task.delay(0.5, function()
				if grabtoolsFunc then grabtoolsFunc:Disconnect() end
				isDupeOnCooldown = false
				DupeToolsBtn.Text = "Dupe Tools"
				DupeToolsBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
			end)
		end)
	else
		if camDummy then camDummy:Destroy() end
		cam.CameraSubject = newHumanoid or char
		isDupeOnCooldown = false
		DupeToolsBtn.Text = "Dupe Tools"
		DupeToolsBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
	end
end)
